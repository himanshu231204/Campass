import React from "react"

export const LinearGradient = ({ children }: { children: React.ReactNode }) => {
  return React.createElement(React.Fragment, null, children)
}

export default { LinearGradient }
